def lockOnEditBegins(obj, event) -> None:
    """Lock the object when a user start working on the object"""

def unlockAfterModification(obj, event) -> None:
    """Release the DAV lock after save"""
