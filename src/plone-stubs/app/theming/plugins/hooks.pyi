def onStartup(event) -> None:
    """Call onDiscovery() on each plugin for each theme on startup"""

def onRequest(object, event) -> None:
    """Call onRequest() on each plugin for the enabled theme on each request"""
