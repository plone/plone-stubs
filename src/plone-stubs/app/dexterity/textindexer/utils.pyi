def searchable(iface, field_name) -> None:
    """
    mark a field in existing iface as searchable
    """

def no_longer_searchable(iface, field_name):
    """Removes a "searchable" mark from a previously marked
    field.
    """
